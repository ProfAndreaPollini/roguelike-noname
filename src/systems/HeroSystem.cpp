//
// Created by pollini on 02/02/2022.
//

#include "HeroSystem.h"
void HeroSystem::updateHero() {
    //    auto& ecs = Services::Ecs::ref();
    //
    //    auto hero = ecs.registry.view<HeroTag>().front();
    //
    //    auto& actions = ecs.registry.get<Actions>(hero);
    //    for (auto& action : actions.commands) {
    //        action->execute(hero);
    //    }
    //    actions.commands.clear();
}
