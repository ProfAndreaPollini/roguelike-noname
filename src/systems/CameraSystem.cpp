//
// Created by pollini on 02/02/2022.
//

#include "CameraSystem.h"

#include "Services.h"
#include "components/Viewport.h"

[[maybe_unused]] void CameraSystem::updateCamera(int col, int row) {
    // TODO: implement
}

void CameraSystem::updateViewport() {}

void CameraSystem::createCamera(int col, int row, int width, int height) {
    //    auto& ecs = Services::Ecs::ref();
    //    auto entity = ecs.registry.create();
    //
    //    auto& viewport = ecs.registry.emplace<Viewport>(entity);
    //    viewport.col = col;
    //    viewport.row = row;
    //    viewport.width = width;
    //    viewport.height = height;
}